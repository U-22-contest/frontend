name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'

    - name: Install dependencies
      run: pnpm install
      working-directory: ./actual/path/to/your/project # ここを修正

    - name: Run build
      run: pnpm build
      working-directory: ./actual/path/to/your/project # ここを修正

    - name: Debug Directory
      run: ls -la
      working-directory: .

